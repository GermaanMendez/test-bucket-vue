name: Deploy Vue App to S3

on:
  push:
    branches:
      - main  

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout source code
      uses: actions/checkout@v3


    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: 18

    - name: Install dependencies
      run: npm install

    - name: Build Vue.js app
      run: npm run build

    - name: read variables
      run: echo "Bucket name is ${{ secrets.S3_BUCKET_NAME }}  AWS acces key is ${{ secrets.AWS_ACCESS_KEY_ID }}"


    - name: Deploy to S3
      uses: jakejarvis/s3-sync-action@v0.5.1
      with:
        args: --acl public-read --delete
      env:
        AWS_S3_BUCKET: "test-vue-gm"
        AWS_ACCESS_KEY_ID: "ASIA2MAWD564UJ3URUG6"
        AWS_SECRET_ACCESS_KEY: "TgDhN9b2P6YcWzawVNKOSwmnzwupspR//nkaXrl9"
        AWS_REGION: "us-east-1"
        SOURCE_DIR: dist 
